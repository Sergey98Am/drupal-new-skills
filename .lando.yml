name: lando-new-skills
recipe: drupal9
config:
  webroot: web

services:
  phpmyadmin:
    type: phpmyadmin
    hosts:
      - database

proxy:
  phpmyadmin:
    - "phpmyadmin.linguasky-d9.lndo.site"
